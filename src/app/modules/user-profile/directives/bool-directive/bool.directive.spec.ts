import { BoolDirective } from './bool.directive';

describe('BoolDirective', () => {
  it('should create an instance', () => {
    const directive = new BoolDirective();
    expect(directive).toBeTruthy();
  });
});
